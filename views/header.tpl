<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>It's Twitasmagorical, yo!</title>

    <!-- Bootstrap core CSS -->
    <link href="{if !isset($index)}../{/if}css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="{if !isset($index)}../{/if}css/cover.css" rel="stylesheet">
    <link href="{if !isset($index)}../{/if}css/lightbox.css" rel="stylesheet">

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="{if !isset($index)}../{/if}js/lightbox.min.js"></script>

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="{if !isset($index)}../{/if}js/bootstrap.min.js"></script>
    <script src="{if !isset($index)}../{/if}js/docs.min.js"></script>
  </head>
